#!/bin/bash

# makes sure the folder containing the script will be the root folder
cd "$(dirname "$0")" || exit

# This script displays various information to the screen
# Run `$ bash hello-shell.sh` in your terminal

# Display 'Hello'
echo "Hello Shell"
